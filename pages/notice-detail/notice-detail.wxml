<!-- pages/notice-detail/notice-detail.wxml -->
<view class="notice-detail-page" wx:if="{{detail}}">
  <view class="title">{{detail.title}}</view>
  <view class="time">{{detail.created}}</view>
  <view class="content">{{detail.content}}</view>
</view>